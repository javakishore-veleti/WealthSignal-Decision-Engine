# WealthSignal-Decision-Engine
Production decision engine for the wealth-management division: PyTorch models for form-completion probability, customer segmentation (context) │ (autoencoder), next-action prediction (LSTM), and Customer-R1 SFT+RL policy — registered in MLflow and served as a FastAPI inference API consumed by downstream offer-generation agents.
